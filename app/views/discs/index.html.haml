%p#notice= notice
%h1 Discs
%table
  %thead
    %tr
      %th{:colspan => "3"}
  %tbody
    - @discs.each do |disc|
      %tr
        %p
          = disc.plastic_type
          = disc.model

        %td= link_to 'Show', disc
        %td= link_to 'Edit', edit_disc_path(disc)
        %td= link_to 'Destroy', disc, method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Disc', new_disc_path
